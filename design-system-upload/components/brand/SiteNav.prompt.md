`SiteNav` is the marketing-site header. The wordmark is always plain Syne text — there is no logo file.

```jsx
<SiteNav links={[{id:"plans",label:"Plans"},{id:"about",label:"About"}]} active="plans" onSelect={go} />
```
