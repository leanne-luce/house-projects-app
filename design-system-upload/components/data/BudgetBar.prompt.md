`BudgetBar` is used in pairs (Estimated, then Actual) inside the Detail hero's budget block.

```jsx
<BudgetBar label="Estimated" value={800} max={800} amount="$800" />
<BudgetBar label="Actual" value={940} max={800} amount="$940" over />
```
