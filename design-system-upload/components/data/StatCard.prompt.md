`StatCard` in an auto-fit grid (`minmax(7rem,1fr)`) forms the Overview summary strip.

```jsx
<StatCard value="$4,120" label="Total spent" />
<StatCard value="$310" label="Over budget" tone="danger" />
```
