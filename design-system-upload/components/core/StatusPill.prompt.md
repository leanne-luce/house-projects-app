`StatusPill` shows a detail's status anywhere it appears in a list, row or hero.

```jsx
<StatusPill status="in_progress" />
```

Only the four statuses exist; do not invent new tones. Text is uppercase, 700, 0.68rem.
