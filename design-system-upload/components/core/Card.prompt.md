`Card` is the default container for any grouped content on a page — lists, forms, panels.

```jsx
<Card pad>
  <div className="section-title">Quick capture</div>
  …
</Card>
```

Leave `pad` off when the card holds full-bleed rows that draw their own padding and dividers.
