`HouseTabs` sits directly under the header on Receipts, Lookbook and Overview.

```jsx
<HouseTabs houses={houses} active={houseId} onSelect={setHouseId} />
```

Outlined, not filled — it must read as level two beneath `TabNav`.
