`TabNav` is the top-level navigation in Club Luce | Studio (Houses, Inbox, Projects, Receipts, Lookbook, Overview).

```jsx
<TabNav tabs={[{id:"houses",label:"Houses"}]} active="houses" onSelect={setTab} />
```

It scrolls horizontally on narrow screens rather than wrapping.
